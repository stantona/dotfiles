- defaults:
    link:
      force: true
- link:
    ~/.oh-my-zsh/custom/debian.zsh:
      path: oh-my-zsh/custom/debian.zsh
- shell:
  - sudo apt update
  - sudo xargs apt install -yq < packages.debian
